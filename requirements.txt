# EnergyAI SDK - Core Dependencies
# Base requirements for the SDK to function

# Core Semantic Kernel integration
semantic-kernel

# Optional web framework support (for application.py)
fastapi
uvicorn
pydantic

# Azure integration for external services
azure-cosmos
azure-monitor-opentelemetry-exporter

# OpenTelemetry and monitoring
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp
opentelemetry-instrumentation-logging
langfuse

# HTTP client for dynamic tool loading
aiohttp

# Development and testing (optional)
pytest
pytest-asyncio
pytest-mock
pre-commit

# Configuration and utilities
pyyaml
python-dotenv

# Type hints and development
typing-extensions
